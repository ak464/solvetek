# دليل النشر وإعداد الإعلانات - SolveTek

هذا الدليل يشرح خطوة بخطوة كيفية رفع موقعك على الويب (Vercel) وتفعيل إعلانات Google AdSense.

## أولاً: الحصول على معرف إعلانات Google (AdSense ID)

قبل النشر، يجب أن يكون لديك حساب في Google AdSense.

1. اذهب إلى [Google AdSense](https://adsense.google.com/).
2. سجل الدخول أو أنشئ حساباً جديداً.
3. من القائمة الجانبية، اذهب إلى **Account** (الحساب) -> **Settings** (الإعدادات) -> **Account Information** (معلومات الحساب).
4. انسخ الرقم الموجود أمام **Publisher ID** (معرف الناشر).
   - الصيغة تكون مثل: `pub-1234567890123456`
   - **ملاحظة:** في الإعدادات، سنحتاج لإضافة `ca-` قبلها لتصبح `ca-pub-1234567890123456`.

## ثانياً: تجهيز المشروع للنشر

تأكد من أنك قمت بتجربة الموقع محلياً وأن كل شيء يعمل (كما فعلنا سابقاً).

## ثالثاً: النشر على Vercel (موصى به)

أسهل وأسرع طريقة لنشر مواقع Next.js.

1. اذهب إلى [Vercel.com](https://vercel.com) وسجل حساباً (يمكنك استخدام GitHub).
2. قم بربط حساب GitHub الخاص بك وارفع المشروع عليه (Push to GitHub) إذا لم تفعل ذلك بعد.
3. في لوحة تحكم Vercel، انقر على **"Add New..."** ثم **"Project"**.
4. اختر مستودع مشروعك (Repository) واضغط **Import**.

### إعداد متغيرات البيئة (Environment Variables)

**مهم جداً:** في صفحة الإعداد قبل الضغط على Deploy، يجب إضافة المتغيرات التالية في قسم **Environment Variables**:

| الاسم (Key) | القيمة (Value) | الوصف |
| :--- | :--- | :--- |
| `NEXT_PUBLIC_SUPABASE_URL` | `https://...` | رابط مشروع Supabase الخاص بك |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `eyJ...` | مفتاح Anon Key من Supabase |
| `NEXT_PUBLIC_ADSENSE_CLIENT_ID` | `ca-pub-XXXXXXXX` | معرف إعلانات قوقل (لا تنسَ إضافة `ca-` في البداية) |
| `NEXT_PUBLIC_GA_MEASUREMENT_ID` | `G-XXXXXXXX` | (اختياري) معرف Google Analytics |

5. اضغط على **Deploy**.
6. انتظر دقيقة أو اثنتين، وسيصبح موقعك مباشراً! 🚀

## رابعاً: إضافة دومين (نطاق) خاص

بعد النشر، سيعطيك Vercel رابطاً مجانياً (مثل `project.vercel.app`). لربط دومينك الخاص (مثلاً `solvetek.net`):

1. في إعدادات المشروع في Vercel، اذهب إلى **Settings** -> **Domains**.
2. اكتب اسم الدومين الخاص بك واضغط **Add**.
3. سيعطيك Vercel بيانات DNS (قيم A Record أو CNAME) لتقوم بإضافتها في لوحة تحكم مزود الدومين الخاص بك (GoDaddy, Namecheap, etc.).

## خامساً: تفعيل الإعلانات رسمياً

بعد نشر الموقع وربط الدومين:

1. ارجع إلى حسابك في **Google AdSense**.
2. اذهب إلى **Sites** (المواقع) -> **New Site** (موقع جديد).
3. اكتب رابط موقعك الجديد.
4. سيطلب منك وضع كود في الموقع. **لقد قمنا بذلك بالفعل!** لا تحتاج لإضافة أي أكواد يدوياً.
5. اضغط على **Request Review** (طلب المراجعة).
6. ستستغرق قوقل فترة (من أيام لأسبوعين) لمراجعة موقعك والموافقة عليه. بعد الموافقة، ستبدأ الإعلانات بالظهور تلقائياً في الأماكن التي حددناها.

---

## 💡 نصائح للقبول في AdSense
* تأكد من وجود محتوى كافٍ (مقالات مفيدة وحصرية).
* تأكد من وجود صفحات أساسية (سياسة الخصوصية، اتصل بنا، من نحن).
* تأكد من سهولة التنقل في الموقع (وهو ما تم انجازه بالمشروع).

**مبروك! موقعك جاهز للانطلاق والربح. 🎉**
